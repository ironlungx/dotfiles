[global]
    font = Iosevka 8
    frame_width = 2
    separator_height = 2
    padding = 10
    horizontal_padding = 20
    line_height = 0
    separator_color = frame   
    transparency = 5
    corner_radius = 6
    dmenu = /usr/bin/rofi -dmenu
    origin = bottom-center
    icon_theme = Oranchelo
    enable_recursive_icon_lookup = true

    # Background color
    background = "#303446"
    
    # Foreground color
    foreground = "#c6d0f5"

    # Frame color
    frame_color = "#8caaee"

    # Separator color
    separator_color = "#babbf1"

    # Highlight color (for URLs and actions)
    highlight = "#ca9ee6"
